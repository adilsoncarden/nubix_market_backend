package com.nubix.market;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Prueba de integración base generada automáticamente por Spring Boot.
 * Valida que el contexto completo de la aplicación (IoC Container) 
 * pueda inicializarse sin arrojar errores de dependencias o configuraciones.
 */
@SpringBootTest
class MarketBackendApplicationTests {

	@Test
	void contextLoads() {
		// Si la aplicación no puede arrancar (ej. error en application.properties), esta prueba fallará.
	}

}
