package com.nubix.market.enums;

/**
 * Modalidades de entrega disponibles para un pedido.
 *
 * @author Grupo de Desarrollo Nubix Market
 * @version 1.0.0 (2026)
 */
public enum TipoEntrega {
    /** Recojo rápido en tienda (fast lane). */
    FAST_LANE,
    /** Entrega a domicilio. */
    DELIVERY,
    /** Atención presencial en punto de venta. */
    PRESENCIAL
}
