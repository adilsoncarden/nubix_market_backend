package com.nubix.market.enums;

/**
 * Define los diferentes canales a través de los cuales se puede originar y registrar una venta.
 */
public enum CanalVenta {
    /** Venta realizada físicamente en el mostrador del local. */
    PRESENCIAL,
    /** Venta realizada a través de la plataforma web de la tienda. */
    WEB
}
